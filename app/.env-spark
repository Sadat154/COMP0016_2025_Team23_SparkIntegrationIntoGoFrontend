APP_TITLE=IFRC GO
APP_ENVIRONMENT=testing

# must be a URL, protocol required, TLD not required (can be localhost)
APP_API_ENDPOINT=http://localhost:8000

# required tokens/keys – replace with real ones
APP_MAPBOX_ACCESS_TOKEN=REQUIRED FOR SPARK MAPS TO LOAD
APP_TINY_API_KEY=temp-tiny-api-key

# must be a URL with protocol and a valid domain
APP_RISK_API_ENDPOINT=https://risk-api.dev.ifrc.org/